export type ICatalog = Record<string, number>;

export type IOptions = {
  // Element tag to use for the paths
  tag?: string;
  // Class prefix to use for the paths' classname
  classPrefix?: string;
};
